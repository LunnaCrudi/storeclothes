var nome = 'Crudi'  // string - usada para texto e palavras
var idade = 27      // int - número
var semdefinicao    // undefined - indefinido
var nula = null     // objeto (sem nenhum objeto)

const nota = 0              // cont é uma variavel constatne que não pode ser alterada - int
const sobrenome = 'Castro'  // string

let x = 3                   // int
let jogo = 'Rodrigo'        // string
let icia                    // undefined

// Operadores Lógicos

// = (atribuição)
// == (igualdade)
// === (igualdade estrita)
// && (and) - exemplo: só entra no site se for maior de 18
// || (or)
// ! (not)

x = y            // atribui y a x
var var1 = 3     // atribui 3 a variavel 1
"3" == var1      // true
3 == '3'         // true
3 === var1       // true
3 === '3'        // false 